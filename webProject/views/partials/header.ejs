<!-- success and error message -->
<style>
    .su_er{
        width: 150px;
    }
  </style>
<% if (messages.success) { %>
  <div class="su_er" style="color: white; background: green;"><%= messages.success %></div>
<% } %>
<% if (messages.error) { %>
  <div class="su_er" style="color: white; background: red;"><%= messages.error %></div>
<% } %>

<!-- Real-time date and time display -->
<div id="datetime" style="background-color: darkmagenta; color: beige; text-align: center; padding: 5px;"></div>

<script>
    // Function to update date and time
    function updateDateTime() {
        const now = new Date();
        const options = {
            weekday: 'long',
            year: 'numeric',
            month: 'long',
            day: 'numeric',
            hour: '2-digit',
            minute: '2-digit',
            second: '2-digit'
        };
        const datetimeString = now.toLocaleDateString('en-US', options);
        document.getElementById('datetime').textContent = datetimeString;
    }

    // Update date and time every second
    setInterval(updateDateTime, 1000);

    // Initialize on page load
    updateDateTime();
</script>

<!-- real time notification -->
<script src="/socket.io/socket.io.js"></script>
<script>
    const socket = io();
    socket.on('appointmentUpdate', (data) => {
        alert(`Your appointment with ${data.providerName} has been ${data.status}`);
    });
</script>
