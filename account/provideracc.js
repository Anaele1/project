const express = require('express');
const providerac = express.Router();
//const db = require('./database');

//PROVIDERS ACCOUNTS
//all activities of providers; from managing account profiles to selecting accepting patients, deleting account, booking appointment
//and cancelling apointment etc.


module.exports = providerac;